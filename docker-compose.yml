services:
  analytics-service:
    build: 
      context: .
      dockerfile: .
    
    env_file:
      - .env
    
    networks:
      - backend-networks
    
    ports:
      - "8008:8000"
    
    restart: unless-stopped